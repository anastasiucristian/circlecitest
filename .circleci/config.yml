version: 2.1 # using 2.1 provides access to orbs and other features

jobs:
  build:
    working_directory: ~/circlecitest/api
    docker:
      - image: 'circleci/node:14.16.0'
    steps:
      - checkout
      - run: npm install
      - run: npm test
